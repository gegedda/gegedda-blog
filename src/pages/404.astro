---
import NotFound from '../components/NotFound.astro';

/**
 * 让「没有路由匹配」的请求得到一个真正的 404 状态码。
 *
 * 这个文件的存在本身就是功能。SSR 下它的作用是：任何**没有被任何路由匹配**
 * 的路径由框架渲染这个页面，并且状态码是 404。少了它，未知路径会退回成
 * 「返回首页 + 200」的软 404，搜索引擎会把无数个重复首页收进索引。
 *
 * 内容在 `components/NotFound.astro` —— 因为还有第二个入口：路径匹配上了
 * `[...page]` 那个 rest 路由、但页码不合法时（`/foo/`），要由那个页面自己
 * 设状态码并渲染同一个组件。两处必须是同一份内容。
 *
 * **上线后要用 `curl -I` 确认返回的是 404 而不是 200。** 这个页面存在的
 * 唯一目的就是那个状态码，而它是可以静默失效的：`Astro.rewrite()` 会把
 * 状态码强制设回 200（node_modules/astro/dist/core/rewrites/handler.js 里
 * `state.status = 200`），页面看起来完全正常。
 */
---

<NotFound />
