# 静态资源响应头（public/ 下的文件会原样复制到产物根目录）
#
# 这是 Workers Static Assets 的 `_headers` 特性，不是 Pages 的。
# 改造前这里写的是「Cloudflare Pages 响应头配置」——同一个文件、同一套语法，
# 但现在由 Worker 的静态资源服务读取。已实测生效（CSS 拿到 immutable，
# 且响应里带 CF-Cache-Status: HIT，说明确实进了边缘缓存）。
#
# 这个文件**只管静态资源**。动态响应（HTML、feed、sitemap）不走这里，
# 它们由 src/middleware.ts 和各路由自己设置。

# /_astro/ 下的文件名含内容哈希，内容一变文件名就变，因此可以放心长期缓存。
# 不配置的话默认是 max-age=0, must-revalidate，等于每个访客每张图都回源校验一次。
/_astro/*
  Cache-Control: public, max-age=31536000, immutable

# /hero/ 是封面图，路径**不含哈希**——这是从改造前的一处退化，
# 必须显式补一个缓存头。
#
# 改造前封面图走 `<Image>`，产物落在 /_astro/ 下、带内容哈希，
# 于是上面那条 immutable 规则顺带把它也覆盖了。现在图片在 public/hero/ 下，
# 路径是稳定的 /hero/blog-placeholder-1.jpg，匹配不到 /_astro/*，
# 默认就是 max-age=0, must-revalidate —— 每个访客每次访问都要为封面图
# 回一次源。封面图是文章页的 LCP 元素，这条直接打在 G3（LCP < 2.5s）上。
#
# 用 1 周而不是 immutable：路径稳定意味着同名文件**可能被换掉**
# （换一张封面图还是叫那个名字）。一周是"重复访问基本都命中、
# 换了图最多一周后全量生效"的折中。P6 上 R2 之后，地址由内容决定，
# 这条就可以删掉。
/hero/*
  Cache-Control: public, max-age=604800
