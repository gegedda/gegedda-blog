# Cloudflare Pages 重定向。语法：<来源> <目标> <状态码>，取第一条命中的规则。
#
# 这是整次改造里唯一「改错了没法回滚」的地方：代码侧只有 postUrl() 一行，
# 但外部世界（搜索索引、书签、订阅器）已经记住了旧地址。站点才上线不久，
# 代价接近零，留着当保险。
#
# 301 是永久重定向，浏览器会长期缓存，确认无误后再上线。

# 旧的列表页 /blog/ 要回到首页，不是 /posts/：
# 列表现在就在 `/`，而 `/posts/` 下只有详情页，指过去会 404。
# 必须排在 /blog/* 前面——否则被通配吃掉，splat 为空 → /posts/。
/blog/    /              301

# 文章从 /blog/<slug>/ 迁到 /posts/<slug>/
/blog/*   /posts/:splat  301

# 订阅地址改名
/rss.xml  /feed.xml      301
