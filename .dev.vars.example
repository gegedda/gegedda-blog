# Worker 运行时密钥的模板。复制成 .dev.vars 后填真实值：
#
#     cp .dev.vars.example .dev.vars
#
# .dev.vars 已被 .gitignore 忽略，不会进仓库；这个 .example 文件才是入库的那份。
#
# ⚠️ 为什么不是 .env：
#     .env      → 构建期，Astro/Vite 通过 import.meta.env 读取
#     .dev.vars → 运行期，wrangler dev 通过 env 读取
# 往 .env 里填 Worker 密钥，本地 wrangler dev 读不到，会得到一个
# 「本地当成没配置、线上却是好的」的假象，非常难查。两件事分开写。
#
# 线上不要用这个文件：用 `wrangler secret put <名字>` 写入，
# 或者写进 wrangler.toml 的 [vars]（那里只放非密钥的值）。
#
# ─────────────────────────────────────────────────────────────────
# 目前这里是空的。
#
# 后台认证用到的 password_hash / session_secret / session_epoch 按设计
# 存在 D1 的 settings 表里，不是环境变量（见计划 §6），所以不列在这。
# 这个文件先建立约定，等真正出现运行时密钥时再往里加——
# 不要为了填满模板而发明变量名。
